<!--
SOP 空白範本 — 一隻 API 一份檔。
放哪：<專案>/docs/sops/<組名>/<這隻api>.md，例 docs/sops/account/delete-account.md
（組名自己取：account、inventory…；同一組的 API 各自一份檔、放同一個資料夾）
複製這個檔案照著填，填完把所有 > 提示 刪掉。
怎麼填看 sop-authoring-guide.md。原則：能寫多詳細就多詳細——SOP 沒寫的，開發就不知道。
-->

# <這隻 API 的名字：一句話>
> 提示：例「刪除使用者帳號」。

## 這隻 API 做什麼（講清楚，不要只寫一句話）
> 提示：做什麼、給誰用、什麼時候會用到、做了之後對系統/業務的影響。
> 例「客服在使用者申請刪除帳號時使用。刪除後帳號不能再登入、個資會清除，
> 但訂單紀錄依法規要保留。一天大約用到十次。」

## 輸入（每個參數一列，能寫多細就多細）

| 參數名稱 | 型態 | 必填？ | 預設值 | 輸入限制／要先檢查什麼 | 說明 |
|---|---|---|---|---|---|
| user_id | 文字 | 必填 | — | 不能空白；必須是存在的使用者 | 要刪的帳號 |
| notify | 是/否 | 選填 | 是 | — | 要不要通知使用者 |

> 提示：型態用你會的話寫就好：文字／整數／小數／是-否／日期時間／清單。
> 「輸入限制」寫要不要先檢查值：不能是空的？要是正數？有固定格式（像 Email）？超出範圍怎麼辦？
> 參數名稱寫不出英文就寫中文，開發會取名。上面兩列是範例，換成你的。

## 輸出（做完回傳/得到什麼，每個欄位一列）

| 欄位名稱 | 型態 | 說明 |
|---|---|---|
| user_id | 文字 | 被刪的帳號 |
| deleted_at | 日期時間 | 什麼時候刪的 |

> 提示：查詢類＝會查到的欄位**全部列出來**；變更類＝做完會回什麼。上面是範例，換成你的。

## 開始前先確認（precheck，選填，有幾條就依序列幾條）
> 提示：動手前業務上要先確認什麼。例「操作者要有管理員權限」「這個帳號沒有未結清的訂單」。

## 步驟（依序，寫詳細——每步做什麼、怎麼做講清楚）
> 提示：這是整份 SOP 的核心，越詳細 AI 越不用猜。人工操作時你怎麼做，就怎麼寫；
> 一步寫不清楚就拆成兩步。每一步可以帶自己的「做之前確認」「做完確認」，沒有就不寫。
1. <這步做什麼、怎麼做>
   - 做之前先確認（選填）：
   - 做完怎麼確認（選填）：
2. <這步做什麼、怎麼做>（照上面複製，幾步就幾條）

> 提示：例「1. 把帳號狀態改成停用（做完確認：不能再登入） 2. 寄刪除通知信到帳號的 Email
> 3. 清除帳號的個資欄位，訂單紀錄不動（做完確認：帳號查不到、訂單還查得到）」。

## 全部做完，怎麼算成功（postcheck，選填）
> 提示：例「帳號查不到了、狀態變已刪除、使用者有收到通知」。

## 危險嗎/能反悔嗎/要誰批准
> 提示：會不會無法復原、要不要人簽核。不能反悔的一定要寫要誰批准、憑什麼批准（例：變更單號）。

## 可能出什麼錯、現場怎麼處理（含哪一步出錯怎麼辦）
> 提示：用平常的話。例「查無此帳號→回報找不到；帳號還有未結訂單→擋下不刪；通知寄不出去→照樣刪，記下來補寄」。

## 測試例子（2–3 個：這樣輸入 → 該得到這樣結果）
> 提示：別跳過，這幾個例子是 AI 驗證自己有沒有做對的依據。輸入用上面「輸入」表的參數寫。
> 例「user_id=沒訂單的帳號 → 成功，帳號消失」「user_id=有未結訂單的帳號 → 被擋，帳號還在」。

## 要留什麼紀錄、給誰看
> 提示：要記什麼。例「記操作人、時間、刪了哪個帳號」。
